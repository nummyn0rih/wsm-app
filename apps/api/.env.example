DATABASE_URL="postgresql://wsm:wsm@localhost:5435/wsm?schema=public"
JWT_SECRET="dev-secret-change-me"
PORT=3001
# Где хранить PDF актов (StorageProvider = disk)
STORAGE_DIR="../../storage"
# В проде true (https). Локально false.
COOKIE_SECURE=false
# Разрешённый origin фронта для CORS/cookie
WEB_ORIGIN="http://localhost:5173"
